import { StyledSafeAreaView, StyledText, StyledView } from './App.styled';

const App = () => (
  <StyledSafeAreaView>
    <StyledView>
      <StyledText>Hello World!</StyledText>
    </StyledView>
  </StyledSafeAreaView>
);

export default App;
